# Copy to .env (or .env.properties) and fill in your OpenRouter key
# (loaded automatically via spring.config.import in application.properties)

OPENROUTER_API_KEY=sk-or-v1-...

# Optional overrides
# OPENROUTER_MODEL=openai/gpt-4o-mini
# OPENROUTER_EMBEDDING_MODEL=qwen/qwen3-embedding-4b
# OPENROUTER_EMBEDDING_DIMENSIONS=2000
# DTH_SERVER_PORT=8080
